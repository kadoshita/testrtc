{
  "name": "testrtc-api",
  "version": "1.0.0",
  "description": "testrtc backend API Worker for Cloudflare Workers",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@skyway-sdk/token": "^1.2.3"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240524.0",
    "@types/uuid": "^10.0.0",
    "typescript": "^5.0.4",
    "wrangler": "^3.0.0"
  }
}
